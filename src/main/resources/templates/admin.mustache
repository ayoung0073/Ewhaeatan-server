<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Admin</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.16.0/umd/popper.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    <script src="/food.js"></script>
</head>
<body>
    <div class="card">
        <div class="card-header">맛집 리스트</div>
        <table id="reply-box" class="list-group" border="1">
            <tr align="center">
                <th width="30">id</th>
                <th width="150">이름</th>
                <th width="100">카테고리</th>
                <th width="70">이화타입</th>
                <th width="280">주소</th>
                <th width="150">대표메뉴</th>
                <th width="130">연락처</th>
                <th width="130">링크주소</th>
                <th width="130">이미지주소</th>
                <th></th>
                <th></th>
            </tr>
            {{#list}}
<!--                <li id="food-{{id}}" class="list-group-item d-flex justify-content-between">-->
                    <tr align="center">
                        <td>{{id}}</td> <!--&nbsp; : 한칸 띄어줌-->
                        <td>{{name}}</td>
                        <td>{{category}}&nbsp;</td>
                        <td>{{ewhaType}}&nbsp;</td>
                        {{#address}}
                            <td>{{address}}</td>
                        {{/address}}
                        {{^address}}
                            <td></td>
                        {{/address}}
                        {{#menu.menuName}}
                            <td>{{menu.menuName}}</td>
                        {{/menu.menuName}}
                        {{^menu.menuName}}
                            <td></td>
                        {{/menu.menuName}}
                        <td>{{phone}}&nbsp;</td>
                        {{#url}}
                            <td><a href="{{url}}">{{url}}</a>&nbsp;</td>
                        {{/url}}
                        {{^url}}
                            <td></td>
                        {{/url}}
                        {{#imageUrl}}
                          <td><a href="{{imageUrl}}">{{imageUrl}}</a>&nbsp;</td>
                        {{/imageUrl}}
                        {{^imageUrl}}
                            <td></td>
                        {{/imageUrl}}
                        <td><button onclick="location.href='/getFood/{{id}}'" class="badge">수정</button></td>
                        <td><button onclick="index.delete({{id}})" class="badge">삭제</button></td>
                    </tr>
<!--                </li>-->
            {{/list}}
        </table>
    </div>
<script src="/js/food.js"></script>

</body>
</html>